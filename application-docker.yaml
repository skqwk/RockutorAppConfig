auth:
  api:
    url: http://auth_cont:8012

kafka:
  enabled: true

spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    bootstrap-servers: http://broker:9092